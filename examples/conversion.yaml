sources:
  - attribute_options
  - products
  - families

conversion:
  data:
    source: products
  actions:
    replace_supplier:
      action: replace
      key: supplier
      source: attribute_options
      method: getLabels
      locale: en_US
    rename_color:
      action: rename
      from: sku
      to: identifier
#    copy_color:
#      action: remove
#      from: colour
#      to: color
#    remove_colours:
#      action: remove
#      keys: [color, colour]
  output:
    writer:
      type: csv
      filename: 'new_products.csv'
      format:
        delimiter: ';'
        enclosure: '"'
      encode_row:
        flatten:
          separator: '-'
